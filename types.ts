
export const UserRole = {
  ADMIN: 'System Administrator',
  USER: 'Normal User',
  STORE_OWNER: 'Store Owner'
};

// Interfaces are removed as they are TypeScript specific.
// JavaScript uses dynamic typing or can use JSDoc for type hinting if needed.
// User, Store, Rating, StoreWithDetails, UserForList interfaces were here.
